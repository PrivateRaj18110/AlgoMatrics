# Anchors pytest's rootdir here so the monorepo root's pyproject.toml pytest
# config (asyncio_mode etc.) does not apply to the ops backend suite.
[pytest]
testpaths = tests
# Declared here rather than inherited: rootdir is deliberately anchored to
# this directory, so the root pyproject's asyncio_mode never reaches us.
asyncio_mode = auto
